1. Open Windows Terminal
2. Open Settings (Ctrl+,)
3. Click "Open JSON file"
4. Add (or modify) optional parameter "startupActions" with your start configuration
  "startupActions": "new-tab -p dxe ; new-tab -p pei"
    where 'dxe' and 'pei' are names of a profiles
5. Save file and reopen WT.
